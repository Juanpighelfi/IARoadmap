# Hoja 01c — Web, HTTP y JavaScript

[Volver al módulo](../01-Niveles/01c%20-%20Web%20HTTP%20y%20JavaScript.md) · [Todas las hojas](README.md)

Abrí esta nota en modo edición en Obsidian y escribí después de **Tu respuesta:**. Todos los apartados están preparados; no tenés que dibujar tablas ni crear formularios. Si una prueba todavía no se ejecutó, escribí «sin ejecutar».

Esta es la hoja del ejercicio, no una segunda bitácora. El código, los datos y las capturas quedan en sus archivos; acá alcanza con enlazarlos. Podés completar el archivo directamente. Si querés conservar respuestas separadas de las actualizaciones del roadmap, duplicá esta hoja una sola vez en `Mi-progreso/` y trabajá en esa copia; esa carpeta necesita respaldo propio.

## 1. Antes de empezar

Explicá dónde se valida un formulario y qué pasa si la API responde con un error. Identificá método, URL, cuerpo, estado y respuesta de una petición observada en las herramientas del navegador. Si no podés, leé las secciones introductorias antes de usar un framework.

**Mi intento o explicación:**

## 2. Ejemplo breve resuelto

Ejemplo resuelto: al enviar un formulario, un botón deshabilitado evita clics repetidos mientras espera. El servidor todavía debe validar y controlar duplicados.

El ejemplo ilustra el formato y el razonamiento; no es un resultado obtenido en tu equipo.

## 3. Práctica principal

En una rama de aprendizaje, usar un componente pequeño del panel React existente para mostrar turnos ficticios: profesional, fecha y estado. Durante este módulo, usar datos locales y una respuesta simulada. Mostrar lista vacía, lista con turnos y error; al enviar, deshabilitar temporalmente el botón y mostrar el resultado. Registrar la petición esperada sin conectar datos reales.

Leer `web/src/admin/api/client.ts` y seguir una llamada hasta Fastify, usando el [mapa del SaaS real](../03-Proyectos/Estudiar%20con%20el%20SaaS%20real.md). Reutilizar el panel; no construir otra aplicación. La simulación no demuestra persistencia ni seguridad del servidor. En 02b se verifica el recorrido con la API existente en un entorno de prueba.

### 1. Componente React que reutilizo y ubicación

**Tu respuesta:**

### 2. Datos ficticios: profesional, fecha y estado

**Tu respuesta:**

### 3. Método HTTP, URL, cuerpo y respuesta esperada

**Tu respuesta:**

### 4. Cómo pasa la petición por web/src/admin/api/client.ts

**Tu respuesta:**

### 5. Estado del componente durante la espera

**Tu respuesta:**

### 6. Validaciones del navegador y comprobaciones que faltan en servidor

**Tu respuesta:**

## 4. Casos y resultados

Completá lo esperado antes de probar. Los campos de resultado quedan pendientes hasta ejecutar el código, hacer el cálculo o revisar el documento correspondiente.

### Lista vacía

**Qué espero antes de ejecutar o calcular:**

**Qué ocurrió realmente (o sin ejecutar):**

**Explicación y archivo/prueba que lo demuestra:**

### Lista con turnos

**Qué espero antes de ejecutar o calcular:**

**Qué ocurrió realmente (o sin ejecutar):**

**Explicación y archivo/prueba que lo demuestra:**

### Cargando

**Qué espero antes de ejecutar o calcular:**

**Qué ocurrió realmente (o sin ejecutar):**

**Explicación y archivo/prueba que lo demuestra:**

### Fallo de red o API

**Qué espero antes de ejecutar o calcular:**

**Qué ocurrió realmente (o sin ejecutar):**

**Explicación y archivo/prueba que lo demuestra:**

### Entrada inválida

**Qué espero antes de ejecutar o calcular:**

**Qué ocurrió realmente (o sin ejecutar):**

**Explicación y archivo/prueba que lo demuestra:**

### Doble clic

**Qué espero antes de ejecutar o calcular:**

**Qué ocurrió realmente (o sin ejecutar):**

**Explicación y archivo/prueba que lo demuestra:**

### Navegación con teclado

**Qué espero antes de ejecutar o calcular:**

**Qué ocurrió realmente (o sin ejecutar):**

**Explicación y archivo/prueba que lo demuestra:**

### Filtro por estado

**Qué espero antes de ejecutar o calcular:**

**Qué ocurrió realmente (o sin ejecutar):**

**Explicación y archivo/prueba que lo demuestra:**

## 5. Variante sin copiar

Agregar un filtro por estado y resolver una respuesta lenta o fallida sin copiar la solución. Tipar el registro en TypeScript y explicar qué errores requieren validación en ejecución aunque los tipos compilen.

### 1. Filtro que agregué sin copiar

**Tu respuesta:**

### 2. Qué hice ante una respuesta lenta

**Tu respuesta:**

### 3. Qué no verifica TypeScript en ejecución

**Tu respuesta:**

## 6. Comprobar y cerrar

Demostrar estados de carga, vacío y error; uso con teclado; envío inválido rechazado por la interfaz; y una explicación del contrato HTTP. Identificar qué comprobaciones faltan del lado servidor. Conservar una captura y el código, no solo una imagen de la pantalla.

**Mi explicación de una decisión, sin consultar:**

**Fallo o límite que queda pendiente:**

**Archivo o comando para repetir la prueba (si aplica):**

No hay que completar otra ficha ni asignarse puntajes. Si querés pedir una corrección, compartí esta hoja y el código o resultado al que hace referencia. En [Mi seguimiento](../00-MOC/Estado%20actual.md) solo anotá el próximo paso.
