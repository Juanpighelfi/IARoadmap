# Modulo 6: MLOps y Produccion

## Flujo
1. `challenges/challenge_1_fastapi_ml.py` - API con FastAPI, Pydantic, logging, Dockerfile
2. `challenges/challenge_2_experiment_tracking.py` - MLflow (o manual), comparar hiperparametros

## Hito
De idea a produccion. API dockerizada, tests, experiment tracking.

## Requisitos
```bash
pip install torch torchvision fastapi uvicorn
# Para tracking: pip install mlflow
# Para containers: docker
```
